{
  "name": "le0n-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:stable": "npm run build && vite preview --host 127.0.0.1 --port 5173 --strictPort",
    "serve:stable": "npm run build && node scripts/stable-preview.cjs"
  },
  "dependencies": {
    "@react-three/drei": "^10.7.7",
    "@react-three/fiber": "^9.6.1",
    "react": "latest",
    "react-dom": "latest",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest"
  }
}
